# Infraestrutura

`IMPLEMENTED` — O repositório oferece configuração local por Docker Compose com MySQL 8.4.11, Prisma e comandos de readiness.

`IMPLEMENTED` — Há validação de ambiente para `local`, `test`, `staging` e `production`; staging/produção exigem credencial na URL do banco.

`UNKNOWN` — Recursos provisionados, provedor, IaC, segmentação de rede, acesso aos ambientes, backups agendados e deployment efetivo.
