# Arquitetura de Dados

`IMPLEMENTED` — O esquema Prisma contém solicitações de crédito, dados de solicitante, histórico de status, formalização, usuários/sessões administrativas, rate-limit de recuperação e ofertas de crédito versionadas.

`IMPLEMENTED` — Dados cadastrais e bancários sensíveis são modelados como campos criptografados; hashes de lookup de CPF e tokens/sessões também aparecem no esquema.

`UNKNOWN` — Classificação formal de dados, retenção aprovada, catálogo, linhagem, exportação e plano operacional de backup em `main`.
